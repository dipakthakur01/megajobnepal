# Frontend .env for cPanel (Vite)
# Rename this to .env.production and build with `npm run build`

# App display
VITE_APP_NAME=MegaJobNepal

# If frontend and backend are on the same domain, you can omit VITE_API_URL.
# The client will call `${origin}/api` automatically in production.
# If backend is on a different subdomain or path, set this explicitly:
# Example: https://api.yourdomain.com/api
# VITE_API_URL=https://yourdomain.com/api

# Optional: Cloudinary or other public keys
NEXT_PUBLIC_CLOUDINARY_CLOUD_NAME=
NEXT_PUBLIC_CLOUDINARY_API_KEY=